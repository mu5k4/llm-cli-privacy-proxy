# Progress

## Completed
- Commit 51a7245 created on August 25, 2026 for the audit artifacts, memory-bank setup, remediation plan, and contributing doc removal.
- Reviewed the repo structure.
- Confirmed the memory-bank directory was missing.
- Extracted project context from the main README and integration docs.
- Created the core memory-bank files.
- Removed the contributing doc because it was redundant with existing README/script guidance.
- Read the audit artifacts that were present during the remediation cycle.
- Converted those audit findings into a prioritized remediation plan during the remediation cycle.
- Began Phase 1 Item 1 in `privacy-service/proxy.py` by replacing `/responses` body pass-through with an explicit top-level allowlist builder and allowlisted header/query forwarding.
- Tightened the nested allowlist so unsupported `tool_choice` object forms, unsupported text formats, and tool objects with extra keys now fail closed.
- Added regression coverage in `scripts/regression.ps1` for accepted simple tool/text shapes and rejected nested allowlist violations.
- Validated the edited source on August 28, 2026 with `python -m py_compile privacy-service/proxy.py`, PowerShell parse validation for `scripts/regression.ps1`, and a stubbed no-rebuild Python source test for the new allowlist logic.
- Finalized the Phase 1 Item 1 design choice on August 28, 2026: keep the nested `/responses` allowlist narrow and fail-closed for now instead of permitting richer tool or structured-output shapes.
- Implemented Phase 1 Item 2 cross-slot canonical scanning in `privacy-service/proxy.py` by removing artificial slot separators, scanning the combined logical text stream, and mapping detected spans back across every touched slot.
- Added a regression case in `scripts/regression.ps1` for a split email spanning adjacent content slots.
- Validated the combined Phase 1 Item 1 and Item 2 source changes on August 28, 2026 with `python -m py_compile privacy-service/proxy.py`, PowerShell parse validation for `scripts/regression.ps1`, and a second stubbed no-rebuild Python source test covering both allowlist and cross-slot behavior.
- Implemented Phase 1 Item 3 protocol-aware restoration on August 28, 2026 by replacing raw whole-stream placeholder substitution with SSE-aware restoration limited to approved passive assistant text fields and non-stream assistant message text/refusal content.
- Added regression coverage in `scripts/regression.ps1` for SSE chunk-boundary restoration inside `response.output_text.delta` and for blocked restoration inside `response.function_call_arguments.delta` and `response.reasoning_text.delta`.
- Validated the combined Phase 1 implementation on August 28, 2026 with `python -m py_compile privacy-service/proxy.py`, PowerShell parse validation for `scripts/regression.ps1`, and a stubbed no-rebuild Python source test covering allowlist behavior, cross-slot detection, SSE-aware restoration, and non-stream response restoration boundaries.
- Implemented Phase 2 Item 4 on August 28, 2026 with a secret-scoped local proxy path generated into `.env`, route registration under `/local/<secret>/...`, authenticated health checks, and delayed Codex provider activation until after stack startup and regression validation.
- Updated PowerShell helpers and docs on August 28, 2026 to use redacted display URLs while keeping the real secret path only in local config/runtime state.
- Validated the local-auth path changes on August 28, 2026 with Python bytecode checks for `privacy-service/app.py` and `privacy-service/proxy.py`, PowerShell parse validation for the updated scripts, PowerShell source validation of generated auth-path URLs, and a stubbed no-rebuild Python route-prefix test confirming that only secret-scoped routes are registered.
- Implemented Phase 2 Item 5 on August 28, 2026 in `scripts/common.ps1` by replacing section-blind Codex config rewrites with root-and-table-aware config transforms, atomic temp-file replacement, parser-backed TOML validation, and UTF-8 no-BOM config writes.
- Validated the Phase 2 Item 5 config hardening on August 28, 2026 with PowerShell parse validation for `scripts/common.ps1` and a fixture-driven install/uninstall config-plan test covering root-vs-scoped `model_provider`, project trust replacement, provider block insertion/removal, and reparsing of the resulting TOML.
- Reviewed Phase 3 Item 6 on August 28, 2026 and confirmed the main remaining gaps are unbounded in-memory sessions, unbounded persistent Presidio cache growth/full-file rewrites, long detector timeouts without stronger budgets, and no top-level `/responses` request-body budget before deep processing.
- Implemented the first Phase 3 Item 6 pass on August 31, 2026 in `privacy-service/app.py`, `privacy-service/proxy.py`, `docker-compose.yml`, and `scripts/regression.ps1` by adding request-body and payload-shape limits, response concurrency gating, bounded session retention with TTL/eviction/delete support, bounded Presidio cache size with atomic writes, and configurable detector/upstream timeouts plus container CPU/memory/PID/read-only limits.
- Validated the Phase 3 Item 6 code path on August 31, 2026 with `python -m py_compile privacy-service/app.py privacy-service/proxy.py` and PowerShell parser validation for `scripts/regression.ps1` and `scripts/common.ps1`.
- Attempted an additional import-based source validation on August 31, 2026, but the local runtime lacked Python dependencies such as `httpx`, so a rebuilt-container regression run is still pending.
- Rebuilt the privacy-service container on August 31, 2026 and iterated on two follow-up fixes discovered by live validation: regression-harness scope repair for `Test-AuthenticatedRouteGuard`, and defensive initialization in `assign_session_token` for plain-dict test sessions.
- Refined the August 31, 2026 container hardening so the analyzer keeps a read-only root filesystem while using tmpfs only for `/tmp` and `/root/.gunicorn`, a workspace-backed cache mount for `/root/.cache`, a longer startup health window, and a higher 3 GB analyzer memory cap to avoid repeated SIGKILL during model boot.
- Completed the rebuilt-container validation on August 31, 2026: `docker compose build privacy-service`, `docker compose up -d privacy-service presidio-analyzer`, and `powershell -ExecutionPolicy Bypass -File .\\scripts\\regression.ps1` all converged, and the regression suite passed.
- Implemented Phase 3 Item 9 on August 31, 2026 in `privacy-service/app.py`, `privacy-service/proxy.py`, and `scripts/regression.ps1` by replacing caller-controlled session creation with server-generated session IDs plus high-entropy session secrets, requiring that secret for protect-session reuse, restore, and delete, and making restore one-shot by default.
- Added Phase 3 Item 9 regression coverage on August 31, 2026 for missing-secret denial, wrong-secret denial, nonexistent-session reuse denial, and post-restore invalidation.
- Validated Phase 3 Item 9 on August 31, 2026 with `python -m py_compile privacy-service/app.py privacy-service/proxy.py`, PowerShell parser validation for `scripts/regression.ps1`, `docker compose build privacy-service`, `docker compose up -d privacy-service`, and `powershell -ExecutionPolicy Bypass -File .\\scripts\\regression.ps1`, which passed.
- Implemented Phase 3 Item 11 on August 31, 2026 in `privacy-service/proxy.py` and `docker-compose.yml` by replacing raw chunk-only Presidio cache keys with fingerprinted cache keys that include cache key version, runtime fingerprint, language, and chunk hash, and by mounting `VERSION` plus `analyzer-config.yaml` into the privacy-service container so security-relevant config/version changes invalidate stale cached empty results.
- Added Phase 3 Item 11 regression coverage on August 31, 2026 in `scripts/regression.ps1` for cache eviction plus fingerprint-based invalidation of cached empty results after a security-relevant fingerprint change.
- Validated Phase 3 Item 11 on August 31, 2026 with `python -m py_compile privacy-service/app.py privacy-service/proxy.py`, PowerShell parser validation for `scripts/regression.ps1`, `docker compose build privacy-service`, `docker compose up -d privacy-service`, and `powershell -ExecutionPolicy Bypass -File .\\scripts\\regression.ps1`, which passed.
- Implemented Phase 4 Item 8 on August 31, 2026 in `privacy-service/app.py` by replacing drop-on-overlap span resolution with union-preserving overlap groups that keep the full covered character range while still selecting a single representative label by the existing priority order.
- Added Phase 4 Item 8 regression coverage on August 31, 2026 in `scripts/regression.ps1` for both nested and partially overlapping spans, verifying that outer sensitive characters are preserved in the merged replacement range.
- Validated Phase 4 Item 8 on August 31, 2026 with `python -m py_compile privacy-service/app.py privacy-service/proxy.py`, PowerShell parser validation for `scripts/regression.ps1`, `docker compose build privacy-service`, `docker compose up -d privacy-service`, and `powershell -ExecutionPolicy Bypass -File .\\scripts\\regression.ps1`, which passed.
- Implemented Phase 4 Item 10 on August 31, 2026 in `privacy-service/proxy.py` by replacing the old fixed 256-character assumption with explicit chunk-size and overlap policy controls, adding a fail-closed guard for boundary-crossing uninterrupted entities longer than the supported visibility window, and folding the chunking policy into the Presidio cache fingerprint.
- Added Phase 4 Item 10 regression coverage on August 31, 2026 in `scripts/regression.ps1` for below-limit, equal-limit, and above-limit entities around the 4,000-character split, including fail-closed behavior for above-limit cross-boundary entities.
- Validated Phase 4 Item 10 on August 31, 2026 with `python -m py_compile privacy-service/app.py privacy-service/proxy.py`, PowerShell parser validation for `scripts/regression.ps1`, `docker compose build privacy-service`, `docker compose up -d privacy-service`, and `powershell -ExecutionPolicy Bypass -File .\\scripts\\regression.ps1`, which passed.
- Implemented Phase 5 Item 7 on August 31, 2026 in `privacy-service/Dockerfile`, `privacy-service.lock`, `Dockerfile.analyzer`, `analyzer.lock`, `analyzer-config.yaml`, and `scripts/regression.ps1` by pinning privacy-critical container images by digest, switching both Python installs to exact hash-locked dependency sets, pinning the GLiNER model snapshot and checksum, pinning the required `microsoft/mdeberta-v3-base` tokenizer/config snapshot and checksums, and rewriting the bundled `gliner_config.json` to the local pinned model path for true offline analyzer startup.
- Validated Phase 5 Item 7 on August 31, 2026 with `docker compose build privacy-service`, `docker compose build presidio-analyzer`, direct offline `GLiNER.from_pretrained('/opt/models/gliner_multi_pii-v1')` loading inside `llm-cli-privacy-analyzer:0.1.0`, `docker compose up -d privacy-service presidio-analyzer`, and `powershell -ExecutionPolicy Bypass -File .\\scripts\\regression.ps1`, which passed.
- Executed the first deferred runtime validation item, `presidio-long-entity-overlap`, on August 31, 2026 against the pinned live stack and found a real proxy-side self-contention bug: multi-chunk Presidio scans were launched concurrently into a one-slot semaphore, producing `429: Presidio scanner is busy` for supported cross-boundary inputs even when the pinned analyzer itself recognized the URL correctly.
- Fixed that deferred-validation finding on August 31, 2026 in `privacy-service/proxy.py` by serializing uncached chunk scans within a single request instead of launching all missing chunks through `asyncio.gather`, and added regression coverage in `scripts/regression.ps1` to ensure a multi-chunk scan no longer self-fails under the bounded scanner semaphore.
- Validated the deferred `presidio-long-entity-overlap` work on August 31, 2026 with `python -m py_compile privacy-service\\proxy.py`, `docker compose build privacy-service`, `docker compose up -d privacy-service`, `powershell -ExecutionPolicy Bypass -File .\\scripts\\regression.ps1`, and a live container harness against the pinned analyzer showing: 1023-char cross-boundary URL -> detected and redacted, 1024-char cross-boundary URL -> detected and redacted, 1025-char cross-boundary URL -> fail-closed `Potential boundary-crossing entity exceeds supported visibility window`.
- Executed the second deferred runtime validation item, `detector-overlap-cooccurrence`, on August 31, 2026 against the pinned live stack with representative overlapping cases and confirmed the current overlap-union behavior end to end. Concrete live detector outputs included: a GitHub token in a URL producing overlapping Presidio URL spans plus Presidio CRYPTO and Nosey Parker SECRET spans that merged to one full outer `SECRET` span; credit-card and IBAN values inside URLs producing nested Presidio `CREDIT_CARD`/`IBAN_CODE` inside Presidio `URL` spans that merged to the full outer URL range; and an email inside a URL producing nested Presidio `EMAIL_ADDRESS` inside a Presidio `URL` span that merged to the full outer URL range.
- Validated the deferred `detector-overlap-cooccurrence` work on August 31, 2026 with a live container harness against the pinned stack plus live `/protect` checks showing `github_token_in_url` -> `Visit GP_SECRET_0001 now`, `card_in_url` -> `Open GP_CREDIT_CARD_0001`, `iban_in_url` -> `Open GP_IBAN_CODE_0001`, and `email_in_url` -> `GP_ORGANIZATION_0001 GP_EMAIL_ADDRESS_0001`, with no raw sensitive outer URL text preserved in the protected output.
- Executed the third deferred runtime validation item, `downstream-restoration-consumers`, on August 31, 2026 against the pinned running stack and confirmed the repo-defined restore boundary with fake placeholder content. Source inspection plus a live SSE harness showed that the proxy restores placeholders only in passive assistant-visible fields: `response.output_text.delta`, `response.output_text.done`, `response.refusal.delta`, `response.refusal.done`, `response.content_part.*` when the part is `output_text` or `refusal`, `response.output_item.done` for assistant message content, and `response.created/completed/incomplete/failed` only through the same assistant message content restoration path.
- Validated the deferred `downstream-restoration-consumers` work on August 31, 2026 by confirming that active protocol fields stayed blocked in the live SSE harness: `response.function_call_arguments.delta` kept `GP_EMAIL_ADDRESS_0001`, `response.reasoning_text.delta` kept `GP_EMAIL_ADDRESS_0001`, and nested non-message fields such as `function_call.arguments`, `reasoning.encrypted_content`, and `id` inside `response.created` remained placeholderized. This repository still does not define external deployed client approval or tool-execution behavior, so that external follow-up remains out of scope for repo-only validation.
- Executed the fourth deferred runtime validation item, `deployed-request-shapes`, on August 31, 2026 against the pinned running proxy and confirmed the currently supported request schema directly from live module state. The allowlisted top-level keys are `background`, `conversation`, `include`, `input`, `instructions`, `max_output_tokens`, `metadata`, `model`, `parallel_tool_calls`, `previous_response_id`, `prompt_cache_key`, `reasoning`, `service_tier`, `store`, `stream`, `stream_options`, `temperature`, `text`, `tool_choice`, `tools`, `top_logprobs`, `top_p`, `truncation`, and `user`. Supported include values are `code_interpreter_call.outputs`, `computer_call_output.output.image_url`, `file_search_call.results`, `message.input_image.image_url`, `message.output_text.logprobs`, `reasoning.encrypted_content`, and `web_search_call.action.sources`. Supported simple tool types are `apply_patch`, `code_interpreter`, `computer_use_preview`, `file_search`, `image_generation`, `shell`, and `web_search_preview`.
- Validated the deferred `deployed-request-shapes` work on August 31, 2026 with live request-builder harnesses in the running proxy. Representative accepted shapes included minimal string input, structured list-based input with `include`, `instructions`, `parallel_tool_calls`, `reasoning.effort`, `stream`, `stream_options.include_obfuscation`, `text.format.type`, `text.verbosity`, `tool_choice`, and simple `tools`, plus a `json_object` text-format case. The harness also confirmed a concrete unsupported shape detail: `stream_options.include_usage` is rejected by the current safe allowlist, so no evidence in this repository supports claiming compatibility with broader external client-emitted stream-options shapes. This repository still lacks a live sanitized capture of actual deployed external client requests.
- Executed the fifth deferred runtime validation item, `scope-inventory-reconciliation`, on August 31, 2026 by reconciling the audited snapshot inventory against the conflicting audit-summary text. The authoritative `artifactsReviewed` list contained 36 entries total, but only 31 were concrete repository paths. The final five entries were aggregate rollups such as `privacy-service dependency and container definitions` and `README, integration guides, handoff documentation, changelog, contribution guidance, version marker, ignore rules, and cache placeholder`, which explained how later summaries could overcount if they treated rollups as additional reviewed items.
- Validated the deferred `scope-inventory-reconciliation` work on August 31, 2026 by indexing all `artifactsReviewed` entries, separating 31 path-like entries from 5 aggregate summary entries, and confirming that the audited snapshot path list itself was internally consistent. The conflicting `29 reviewed files`, `27 reviewed files`, and `31 current-state files` statements were summary-text inconsistencies rather than a mismatch in the authoritative concrete path list. Relative to the current workspace, only `CONTRIBUTING.md` from that original 31-path snapshot inventory was later absent because it was intentionally removed during this remediation cycle.
- Verified the remaining bind-host follow-up on August 31, 2026 with minimal local config checks and live container inspection. `.env.example`, `docker-compose.yml`, and `scripts/common.ps1` all establish the source default `PROXY_BIND_HOST=127.0.0.1`; the current `.env` still sets `PROXY_BIND_HOST=127.0.0.1`; and `docker inspect llm-cli-privacy-proxy` reports the effective published binding as `127.0.0.1:8000`. No widened bind exposure was introduced during this remediation cycle.
- On August 31, 2026, retired the now-historical audit artifact folder and temporary remediation-planning file after all remediation and deferred validation work had been completed and recorded into the core memory-bank files.
- Modernized Python dependency management on August 31, 2026 by adopting a root `pyproject.toml` as the declared dependency source of truth, renaming the install lockfiles to `privacy-service.lock` and `analyzer.lock`, switching both Dockerfiles to the new lockfile paths, removing `privacy-service/requirements.txt`, and validating the migrated build with `docker compose build privacy-service presidio-analyzer` plus `powershell -ExecutionPolicy Bypass -File .\\scripts\\regression.ps1`, which passed.

## Pending
- Keep the memory-bank aligned with future architecture, script, and integration changes.
- Expand decision history as explicit tradeoffs are made.

## Next Steps
- The audit-remediation plan and deferred validation follow-ups are complete; wait for the next user-directed task or explicitly track any new hardening follow-up before making more changes.

